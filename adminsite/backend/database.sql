create TABLE clients(
    id SERIAL PRIMARY KEY,
    name VARCHAR(255),
    surname VARCHAR (255)
    );

create TABLE employees(
    id SERIAL PRIMARY KEY,
    name VARCHAR(255),
    surname VARCHAR (255),
    role VARCHAR (255)
    );
